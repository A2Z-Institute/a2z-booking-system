# Set calendar instructor order

Deploy the package, then run in the Coolify application terminal:

```sh
python set_calendar_instructor_order.py
```

Review the printed names. Then save the order:

```sh
python set_calendar_instructor_order.py --apply
```

Refresh the calendar. The named A2Z order is retained after future logins and
refreshes; remaining staff appear after that list.
