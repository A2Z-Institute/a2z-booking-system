Flask>=3.1,<4
Flask-Login>=0.6.3,<1
python-dotenv>=1.0,<2
waitress>=3.0,<4
Werkzeug>=3.1,<4
openpyxl>=3.1,<4
# Required only by the PostgreSQL migration tools and the future PostgreSQL
# deployment. The current SQLite production deployment continues to work.
psycopg[binary]>=3.2,<4
